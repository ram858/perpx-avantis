# PgBouncer Configuration for PrepX
# Optimized for high-performance trading application

[databases]
# Primary database for writes
prepx_users = host=postgres-primary-us-east port=5432 dbname=prepx_users
prepx_trading = host=postgres-primary-us-east port=5432 dbname=prepx_trading
prepx_portfolio = host=postgres-primary-us-east port=5432 dbname=prepx_portfolio
prepx_market_data = host=postgres-primary-us-east port=5432 dbname=prepx_market_data

# Read-only replicas for reads
prepx_users_ro = host=postgres-replica-us-west port=5432 dbname=prepx_users
prepx_trading_ro = host=postgres-replica-us-west port=5432 dbname=prepx_trading
prepx_portfolio_ro = host=postgres-replica-us-west port=5432 dbname=prepx_portfolio
prepx_market_data_ro = host=postgres-replica-us-west port=5432 dbname=prepx_market_data

# Wildcard database routing
* = host=postgres-primary-us-east port=5432

[pgbouncer]
# Network settings
listen_addr = 0.0.0.0
listen_port = 6432
unix_socket_dir = /var/run/pgbouncer
unix_socket_mode = 0777
unix_socket_group = postgres

# Authentication
auth_type = md5
auth_file = /etc/pgbouncer/userlist.txt
auth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1

# Pooling mode: session, transaction, statement
# session: Client connection is tied to server connection for its lifetime
# transaction: Server connection is released back to pool after transaction
# statement: Server connection is released back to pool after each statement
pool_mode = transaction

# Connection limits
max_client_conn = 1000
default_pool_size = 25
min_pool_size = 5
reserve_pool_size = 5
reserve_pool_timeout = 5

# Database connection limits
max_db_connections = 100
max_user_connections = 100

# Connection management
server_reset_query = DISCARD ALL
server_check_query = SELECT 1
server_check_delay = 30
server_lifetime = 3600
server_idle_timeout = 600
server_connect_timeout = 15
server_login_retry = 15

# Query settings
query_timeout = 300
query_wait_timeout = 120
client_idle_timeout = 0
client_login_timeout = 60

# Logging
log_connections = 1
log_disconnections = 1
log_pooler_errors = 1
log_stats = 1
stats_period = 60

# Admin settings
admin_users = postgres, monitoring
stats_users = postgres, monitoring

# Console settings
console_log_level = 1
console_log_file = /var/log/pgbouncer/pgbouncer.log

# DNS settings
dns_max_ttl = 15
dns_nxdomain_ttl = 15
dns_cache_max_ttl = 900

# Application name settings
application_name_add_host = 1

# Auto-database settings
autodb_idle_timeout = 3600

# SSL settings
ssl = require
ssl_cert_file = /etc/ssl/certs/server.crt
ssl_key_file = /etc/ssl/private/server.key
ssl_ca_file = /etc/ssl/certs/ca.crt
ssl_ciphers = ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256
ssl_prefer_server_ciphers = 1

# Performance tuning
tcp_keepalive = 1
tcp_keepcnt = 3
tcp_keepidle = 600
tcp_keepintvl = 30

# Memory settings
max_packet_size = 2147483647

# Ignore startup parameters
ignore_startup_parameters = extra_float_digits

# Disable prepared statements for better connection pooling
prepared_statement_cache_queries = 0

# Connection validation
server_round_robin = 0

# Logging format
log_fd = 1
syslog = 0

# Performance counters
stats_users = postgres, monitoring
admin_users = postgres, monitoring

# Connection string validation
connection_string_validation = strict

# Pooler statistics
pooler_stats = 1

# Client connection tracking
client_tls_sslmode = prefer
client_tls_cert_file = /etc/ssl/certs/client.crt
client_tls_key_file = /etc/ssl/private/client.key
client_tls_ca_file = /etc/ssl/certs/ca.crt

# Server connection tracking
server_tls_sslmode = require
server_tls_cert_file = /etc/ssl/certs/server.crt
server_tls_key_file = /etc/ssl/private/server.key
server_tls_ca_file = /etc/ssl/certs/ca.crt
